# Test file: Case-insensitive skill key (SKILL instead of skill)

skill:  # Using uppercase to test case-insensitivity
  id: "test_uppercase_skill"
  trigger: "target.distance < 5 && cooldown_ready"
  damage: "10"
  cooldown: 3.0

  effects:
    - type: "SLOWNESS"
      duration: 40
      amplifier: 0

  particle:
    type: "CLOUD"
    count: 10
    offset_x: 0.5
    offset_y: 0.5
    offset_z: 0.5

  sound:
    sound: "entity_player_attack_sweep"
    volume: 1.0
    pitch: 1.2
